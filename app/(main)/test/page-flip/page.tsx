import PhotoBookLoader from "@/components/PhotoBookLoader";

export default function Home() {
  return (
    <div style={{ width: '100%', height: '100vh', overflowX: 'hidden', overflowY: 'hidden' }}>
      <PhotoBookLoader />
    </div>
  );
}
